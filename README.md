# password-generator

A simple password generator cli program written in python. Can output to a file, your clipboard, or stdout.

## Usage

```bash
./password-generator.py
./password-generator.py --number 20 --output-file passwords.txt
```
