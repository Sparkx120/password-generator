[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "password-generator"
version = "0.1.0"

[project.scripts]
password-generator = "password_generator.__main__:main"

[tool.setuptools.packages.find]
where = ["./src/"]
